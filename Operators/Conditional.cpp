// Conditional (Ternary)

#include <iostream>
using namespace std;

int main()
{
    int a = 5;
    int b = 10;

    // Conditional (Ternary) Operator
    cout << "Conditional (Ternary) Operator: " << ((a > b) ? a : b) << endl;

    return 0;
}